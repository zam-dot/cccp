// strings.go
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//#include <ctype.h> 

{{ autoFreeGeneric }}

int main() {
{{ slice_literal "helloWORLD" "2" "5" "result" }}
printf("Sliced: %s\n", result); 

{{ create "TESTING123!" "massive" }}
{{ slice "massive" "2" "5" "output" }}
printf("Sliced: %s\n", output); 

int current_score = 42;      // ← Declare these first!
int max_score = 100;         // ← Declare these first!

{{ string_format "status" "\"Score: %d/%d\"" "current_score" "max_score" }}
printf("Status: %s\n", status);
}
